---
phase: 09-mail-path-and-profile-page-correctness
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - CHANGES.rst
autonomous: true
requirements: [BUG-07, BUG-08, UX-01, UX-02]

must_haves:
  truths:
    - "`bin/test -t '!robot'` is green with all four fixes and all five new test methods in place — the three plans landed in parallel and do not interfere."
    - "`bin/test-coverage -t '!robot'` passes at `--fail-under=90`, so the two previously-uncovered lines at `request_bar_code_reset.py:112-113` no longer cost the milestone its coverage gate."
    - "`bin/code-analysis` exits 0, so the buildout git pre-commit hook keeps passing without `--no-verify`."
    - "Nothing in this phase's diff mutates a resource this package does not own, adds a skin layer, or vendors JavaScript — `imio.dms.mail` still installs in either order."
    - "No MFA state write was added to the PAS plugin or a challenge plugin; every change in this phase is inside a committing view, a helper it calls, a template, or a schema attribute."
    - "The user can copy the base32 secret shown on the enrollment page into a real desktop TOTP client or password manager, and the codes that client then produces are accepted at login."
    - "`CHANGES.rst` records all four fixes and the one accepted consequence Phase 13 has to pick up: the orphaned French translation of the old profile-field description."
  artifacts:
    - "CHANGES.rst — a Phase 9 entry covering BUG-07, BUG-08, UX-01 and UX-02"
  key_links:
    - "the three wave-1 plans touch disjoint production files, so the only place their interaction is observable is the full suite and the coverage number — which is what this plan runs"
---

<objective>
Prove the phase as a whole, and record it.

The three wave-1 plans are independent by construction: BUG-07 is `request_bar_code_reset.py`,
BUG-08 is `userdataschema.py`, UX-01 is `recovery_codes.pt`, UX-02 is `helpers.py`. They share no
production file, so each was verified against its own scoped test run. This plan is where the full
suite, the coverage gate and the lint gate run once over all of them together, where the one
success criterion that cannot be automated is checked by hand, and where the changelog entry lands.

Purpose: the milestone's standing constraints are per-commit conditions, and the coverage gate in
particular is only meaningful over the whole suite. Running it three times in parallel would not
have measured it.

Output: a verified phase and a `CHANGES.rst` entry.

Decisions recorded here: D-16 (the orphaned French translation of the retired profile-field
description is an accepted, deliberate Phase 13 input — the changelog is where a reader outside
`.planning/` learns about it).
</objective>

<execution_context>
@/srv/src/imio.googleauthenticator/.claude/gsd-core/workflows/execute-plan.md
@/srv/src/imio.googleauthenticator/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mail-path-and-profile-page-correctness/09-CONTEXT.md
@.planning/phases/09-mail-path-and-profile-page-correctness/09-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The whole suite, the coverage gate and the lint gate</name>
  <precondition>Plans 09-01, 09-02 and 09-03 are committed; `bin/test`, `bin/test-coverage` and `bin/code-analysis` all exist (buildout has been run).</precondition>
  <files>none — this task changes no file unless a gate fails</files>
  <read_first>
    - .planning/ROADMAP.md (section "Standing constraints for every v1.1 phase" — the six conditions every commit in this milestone has to satisfy)
    - .planning/phases/09-mail-path-and-profile-page-correctness/09-VALIDATION.md (the sampling contract: scoped run per task commit, full suite per wave, coverage gate before verification)
    - the three SUMMARY files this phase has produced so far, for the recorded non-vacuity red outputs
  </read_first>
  <action>
    Run the three gates over the merged phase, in this order, and record each command's exit status
    and the relevant output in the SUMMARY.

    First `bin/test -t '!robot'` — the full suite excluding the robot module, which needs a real
    browser and is excluded everywhere. Five test methods added by this phase must appear in the
    run: the two mail-failure methods, the field-description method, the home-page-link method and
    the secret-as-text method.

    Then `bin/test-coverage -t '!robot'`, which runs the suite under coverage and then reports with
    `--fail-under=90` against `.coveragerc`'s branch-coverage configuration. Record the reported
    percentage in the SUMMARY, not just the pass. The two lines the roadmap records as uncovered
    are the ones BUG-07 replaced, so this number should not have moved down.

    Then `bin/code-analysis`. It must exit 0. Note the flake8-isort caveat recorded in STATE.md:
    findings are reported from a diff, so a file's finding count shifts whenever any line in it
    changes — judge the result by error codes and the exit status, never by a per-file count.

    If any gate fails, do not paper over it: identify which plan's change caused it, fix it in that
    plan's file, and re-run all three from the top. A failure here is a real interaction between
    changes that were verified only in isolation, which is the entire reason this task exists.

    Finally, confirm by inspecting `git diff --name-only` across this phase's commits that the
    changed set is exactly the six files the three plans declared, plus their SUMMARY and PLAN
    documents — no `profiles/` file, no `locales/` file, no `browser/static/` file, no
    `pas_plugin.py`, and no `browser/settings_helper.py`.
  </action>
  <verify>
    <automated>bin/test -t '!robot'</automated>
    <automated>bin/test-coverage -t '!robot'</automated>
    <automated>bin/code-analysis</automated>
  </verify>
  <acceptance_criteria>
    - `bin/test -t '!robot'` exits 0, and the run includes the five test methods this phase added.
    - `bin/test-coverage -t '!robot'` exits 0 at `--fail-under=90`, and the SUMMARY records the reported branch-coverage percentage.
    - `bin/code-analysis` exits 0.
    - `git diff --name-only` across the phase's source commits lists only: `src/imio/googleauthenticator/browser/forms/request_bar_code_reset.py`, `src/imio/googleauthenticator/tests/test_request_bar_code_reset.py`, `src/imio/googleauthenticator/userdataschema.py`, `src/imio/googleauthenticator/tests/test_adapter.py`, `src/imio/googleauthenticator/browser/forms/recovery_codes.pt`, `src/imio/googleauthenticator/helpers.py`, `src/imio/googleauthenticator/tests/test_user_setup.py`.
    - No path under `src/imio/googleauthenticator/locales/`, `src/imio/googleauthenticator/profiles/` or `src/imio/googleauthenticator/browser/static/` appears in that list, and neither does `pas_plugin.py` or `browser/settings_helper.py`.
  </acceptance_criteria>
  <done>All three gates green over the merged phase, with the coverage percentage recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Changelog, and the one check a test cannot make</name>
  <files>CHANGES.rst</files>
  <read_first>
    - CHANGES.rst (the `1.0.0 (unreleased)` section and the entry style — one bullet per change, each closed with a `[chris-adam]` attribution line)
    - .planning/phases/09-mail-path-and-profile-page-correctness/09-VALIDATION.md (the Manual-Only Verifications table — the exact five-step UAT script for the TOTP-client check)
    - the three SUMMARY files this phase produced, for the changes to describe
  </read_first>
  <action>
    Add a Phase 9 entry to `CHANGES.rst` in the existing style, one bullet per fix, each with the
    attribution line the surrounding entries use. Describe behaviour, not line numbers: a refused or
    unreachable mail server now reports itself on the reset form instead of producing an error page;
    the profile field for two-step verification no longer offers set-up and disable links that would
    have acted on the viewing administrator's own account; a user who finishes enrollment is offered
    the site home page rather than their own profile; and the enrollment page now shows the TOTP
    secret as copyable text beside the QR code.

    Add one further bullet recording the accepted consequence (D-16): shortening the profile-field
    description retires its message id, and the French translation of the old text is orphaned until
    the catalogues are rebuilt, so that field reads in English for a French user in the meantime.
    This is the one thing in this phase a reader outside `.planning/` would otherwise be surprised
    by, and it is a deliberate Phase 13 input rather than a defect.

    Then perform the manual verification below. It is the second half of success criterion 4 and it
    is not automatable in this suite: a test can assert the base32 string is rendered and equals the
    stored seed, but it cannot drive a third-party TOTP client. Record the outcome in the SUMMARY,
    including which client was used.
  </action>
  <verify>
    <automated>git diff --stat -- CHANGES.rst</automated>
    <human-check>
      Enrol a test user at `@@setup-two-factor-authentication` on a running instance
      (`bin/instance fg`). Copy the base32 setup key shown beside the QR code — as text, not by
      scanning the image. Enter it into a desktop TOTP client or a password manager as a manually
      entered setup key. Log out, log in, and submit the code that client shows. Confirm the site
      accepts it. Note: `bin/instance` does not have `IMIO_GOOGLEAUTHENTICATOR_SEED_KEY` set —
      `base.cfg:54` sets it for the `[testenv]` section only, and the Puppet fragment that would
      supply it in production has not shipped — so export it in the shell before starting the
      instance, or this check cannot run at all.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `CHANGES.rst` gains a bullet for each of BUG-07, BUG-08, UX-01 and UX-02, plus one recording the orphaned French translation as an accepted Phase 13 input, each following the file's existing bullet-and-attribution style.
    - `git diff --name-only` for this task lists `CHANGES.rst` and nothing else.
    - The SUMMARY records the manual TOTP-client verification: the client used, whether the copied secret was accepted, and the code that was submitted at login being accepted or rejected. A skipped or blocked check is recorded as such, with the reason, never as a pass.
    - `bin/code-analysis` exits 0 (unchanged by a documentation edit, but the pre-commit hook runs on every commit).
  </acceptance_criteria>
  <done>The changelog describes the phase, and the one criterion no test can reach has a recorded human result.</done>
</task>

</tasks>

<assumption_delta_decision>
The assumption-delta detector fired on this phase's scope with three signals: `second` and `another`
(classified `pluralization`) and `selectable` (classified `chosen`).

**Decision: no-change.** All three are lexical false positives, confirmed by re-reading the phase
scope rather than by preference. `second` comes from "second factor", the package's name for the one
authentication factor it adds — not a second instance of anything. `another` comes from "another
user's profile", which describes an existing two-principal situation (viewer and viewed) that BUG-08
*removes* an affordance from; it introduces no new principal and no new identity. `selectable` comes
from "selectable text", a rendering property of a string on a page, not a configuration knob and not
a value promoted from derived to chosen.

No noun changed primacy in this phase. No identity model, primary key or anchor field moved. Nothing
was generalized, so there is nothing to promote and no add-alongside debt to accept. This checkpoint
is advisory and records that outcome; it does not block the phase.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> deployed instance | The changelog is the record a deploying operator reads. An accepted consequence that is not recorded there becomes an unexplained regression report later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Repudiation | undocumented accepted consequence of D-15/D-16 | low | mitigate | Record the orphaned French translation in `CHANGES.rst` so the English fallback on the profile field is a known, attributable decision rather than an unexplained regression discovered by a French-speaking user. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this plan; the only file changed is a changelog. Every pin remains frozen in `test-4.3.cfg`. |
</threat_model>

<verification>
- All three gates recorded green with their output in the SUMMARY.
- The phase's changed-file set matches exactly what the three wave-1 plans declared.
- The manual TOTP-client check has a recorded outcome, pass or otherwise.
</verification>

<success_criteria>
All four Phase 9 success criteria hold, the milestone's six standing constraints are satisfied by
every commit in the phase, and the phase is recorded in `CHANGES.rst`.
</success_criteria>

## Artifacts this phase produces

| Kind | Symbol | File |
|------|--------|------|
| changelog entry | Phase 9 bullets under `1.0.0 (unreleased)` | `CHANGES.rst` |

No new code symbol, no new msgid, no new test method. This plan runs gates and writes prose.

<output>
Create `.planning/phases/09-mail-path-and-profile-page-correctness/09-04-SUMMARY.md` when done.
</output>
