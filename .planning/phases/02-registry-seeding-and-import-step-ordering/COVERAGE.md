No external API integration: this phase edits GenericSetup wiring (`configure.zcml`, `setuphandlers.py`), one local helper (`helpers.get_ska_secret_key`) and tests — no external API or SDK is called.

Detector result for the phase scope (ROADMAP §Phase 2 + 02-CONTEXT.md): `{"detected":false,"signals":[]}`.
