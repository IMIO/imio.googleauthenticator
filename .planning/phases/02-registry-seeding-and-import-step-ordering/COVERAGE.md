No external API integration: this phase edits GenericSetup wiring (`configure.zcml`, `setuphandlers.py`), one local helper (`helpers.get_ska_secret_key`) and the test suite — no external API, SDK, or service is called, added, or wrapped.

Detector result for the phase scope (ROADMAP §Phase 2 + 02-CONTEXT.md): `{"detected":false,"signals":[]}`.
